fn main() {
    /*
        # Cargo is rust's build system and package manager.(eg: download dependencies)
        # cargo version "cargo --version"
        # TOML: Tom's obvious minimal language
        # commands
        # cargo build
        # cargo build --release (optimized)
        # cargo run
        # cargo run --release
        # cargo check
        # cargo check -release
        # cargo new <project_name>
    */
    println!("Hello, world!");
}
